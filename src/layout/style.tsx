import styled from "styled-components";

export const AppContentWrapper = styled.main`
  background-color: var(--hcolor-bg-2);
  margin: 0 auto;
  padding-top: 80px;
  padding-left: 250px;
  padding-right: 20px;

  box-sizing: border-box;
  min-height: 100vh;
`;
