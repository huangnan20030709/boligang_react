import styled from "styled-components";

export const AppContentWrapper = styled.main`
  background-color: var(--color-bg-2);
  margin: 0 auto;
  padding-top: 90px;
  padding-left: 290px;

  box-sizing: border-box;
  min-height: 100vh;
`;
